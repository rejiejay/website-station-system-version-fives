const container = {
    padding: '15px',
    minHeight: '70%',
}

const message = {
    wordWrap: 'break-word',
    wordBreak: 'break-all'
}

const style = {
    container,
    message
}

export default style
